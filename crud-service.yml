server:
  port: 9090

spring:
  jpa:
    hibernate:
      ddl-auto: validate
      format_sql: true
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/escoladb?useSSL=false
    username: root
    password: 1234

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
